function soma(){
    var numero1, numero2, resultado;
    numero1=parseFloat(prompt("Digite uma parcela"));
    numero2=parseFloat(prompt("Digite outra parcela"));
    resultado = numero1+numero2
    alert(numero1+" + "+numero2+" = "+resultado)
}